export interface IAppearance {
  background: string;
  fieldBackground: string;
  accentColor: string;
  textColor: string;
  font: string;
  outlineWidth: string;
  borderRadius: string;
  borderColor: string;
  borderWidth: string;
  logo: string;
}
