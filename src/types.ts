export interface IAppearance {
  background: string;
  fieldBackground: string;
  accentColor: string;
  textColor: string;
  fontFamily: string;
  outlineWidth?: string;
  outlineColor?: string;
  outlineRadius?: string;
  borderRadius: string;
  borderColor: string;
  borderWidth: string;
  logo: string;
  backdropBlur: string;
  backdropColor: string;
  boxShadow: string;
}
